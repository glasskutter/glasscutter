import React from 'react'
import ReactDOM from 'react-dom/client'
import GlassCutter from './glass-cutter'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GlassCutter />
  </React.StrictMode>,
)
